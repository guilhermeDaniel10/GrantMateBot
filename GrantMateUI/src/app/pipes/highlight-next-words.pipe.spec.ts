import { HighlightNextWordsPipe } from './highlight-next-words.pipe';

describe('HighlightNextWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightNextWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
